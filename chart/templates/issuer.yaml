apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "linkerd2-trust-anchor.fullname" . }}
  labels:
    {{- include "linkerd2-trust-anchor.labels" . | nindent 4 }}
spec:
  ca:
    secretName: {{ include "linkerd2-trust-anchor.fullname" . }}-issuer