apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linkd2-trust-anchor.fullname" . }}-test-connection"
  labels:
    {{- include "linkd2-trust-anchor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linkd2-trust-anchor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
